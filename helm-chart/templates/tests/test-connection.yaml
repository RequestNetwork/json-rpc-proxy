apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "json-rpc-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "json-rpc-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "json-rpc-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
